{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "build": "jshint src && pulp test && rimraf docs && pulp docs"
  },
  "devDependencies": {
    "jshint": "^2.8.0",
    "pulp": "^4.0.1",
    "rimraf": "^2.4.1"
  }
}
